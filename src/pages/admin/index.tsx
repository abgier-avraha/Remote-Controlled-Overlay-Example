import { type NextPage } from "next";
import Head from "next/head";

const Home: NextPage = () => {
  return (
    <div>
      <Head>
        <title>Tennis Overlay Client</title>
      </Head>
      Admin Panel
    </div>
  );
};

export default Home;
